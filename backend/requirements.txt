# ── Web Framework ──────────────────────────────────────────────
# Use >= pins for all packages that need Rust/C compilation so pip
# can pick pre-built Python 3.14 wheels instead of building from source.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Validation & Config ────────────────────────────────────────
# pydantic 2.7.1 pulls pydantic-core 2.18.2 which has no py3.14 wheel.
# Latest pydantic ships py3.14 wheels — use >= here.
pydantic>=2.7.1
pydantic-settings>=2.2.1
python-multipart>=0.0.9
python-dotenv>=1.0.1

# ── Database ───────────────────────────────────────────────────
sqlalchemy>=2.0.30
alembic>=1.13.1
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# ── Auth ───────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── HTTP Client ────────────────────────────────────────────────
httpx>=0.27.0

# ── Data / ML ──────────────────────────────────────────────────
numpy>=2.1.0
pandas>=2.2.2
scikit-learn>=1.5.2
xgboost>=2.1.0
joblib>=1.4.2

# ── Infrastructure ─────────────────────────────────────────────
redis>=5.0.4
celery>=5.4.0
structlog>=24.1.0
prometheus-client>=0.20.0
